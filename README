This folder contains the code for the lidar. This code takes in the scan from the hokuyo lidar
and then converts into a ROS pointcloud data type and publishes it as a ROS message. Then we 
run a PassThrough and StatisticalOutlierRemoval filter in order to reduce our data set. 
